#include "main.h"

/**
 * _strlen - a func that returns the length of a string
 * @s: the string in type char
 * Return: 0.
 */
int _strlen(char *s)
{
	int i = 0;

	for (; *s++;)
		i++;
	return (a);
}

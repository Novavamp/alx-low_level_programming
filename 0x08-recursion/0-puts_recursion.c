#include "main.h"

/**
 * _puts_recursion - The function prints a string
 * @s: The string to be printed
 *
 * Return: Always 0.
 */

void _puts_recursion(char *s);
{
	char s;

	_puts_recursion("%s\n", s);
	return (0);
}
